FROM node:18-alpine

WORKDIR /app

# Install dependencies based on the lock file for consistency
COPY package*.json ./
RUN npm install

# Copy the rest of the application files
COPY . .

# Expose the port and start the development server
EXPOSE 3000
CMD ["npm", "start"]